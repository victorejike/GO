package main

import(
	"fmt"
)

func capitalized(text string)string{
	result := strings.ToUpper(test[0:1]) + strings.ToLower(test[1:])
	return result
}

func transforma(text string)string{
  tokens := strings.Fields(text)
  var result []string

  for i := 0; i < len(tokens); i++ {
	 tokens := tokens[i]

	 if tokens == "(up)" && len(result) > 0 {
		
	 }
  }

}